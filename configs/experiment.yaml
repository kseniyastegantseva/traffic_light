simulation:
  duration_seconds: 900
  seeds: [1, 2, 3, 4, 5]
  service_time_seconds: 2.0

intersection:
  lanes:
    north: {arrival_rate_per_minute: 8}
    south: {arrival_rate_per_minute: 8}
    east: {arrival_rate_per_minute: 5}
    west: {arrival_rate_per_minute: 5}
  min_green_seconds: 15
  yellow_seconds: 3

controllers:
  - type: fixed
    phase_duration_seconds: 35
  - type: actuated
    decision_interval_seconds: 10
  - type: ai
    decision_interval_seconds: 10

output:
  path: outputs/compare_results.json
  summary_csv: outputs/compare_summary.csv
